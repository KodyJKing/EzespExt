# IDE to target when generating premake project files
$Config_IDE = 'vs2022'

# Default project to start when running/watching the project. Comment out if you want to be prompted each time.
# $Config_StartProject = 'console-app-2'
